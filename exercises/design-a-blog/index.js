window.onload = function pageSetup(params) {
  var dates = this.document.querySelectorAll('.dateHook')

  // Array.from(dates).forEach(node => console.log(node.innerHTML = `${new Date().getDate()}${new Date().getUTCMonth()}`));
}